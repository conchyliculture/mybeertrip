<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:mybeertrip="http://schemas.android.com/apk/res-auto">
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_add_beer"
          android:icon="@drawable/ic_menu_add"
          android:title="@string/action_add_beer"
          android:orderInCategory="2000"
          mybeertrip:showAsAction="ifRoom|collapseActionView" />
   
</menu>